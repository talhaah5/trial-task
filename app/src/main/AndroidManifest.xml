<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.codeConfirmation">  </activity>
        <activity android:name=".Activity.welcomeback" />
        <activity android:name=".Activity.SignInWithPhone" />
        <activity android:name=".Activity.login" />
        <activity android:name=".Activity.splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Activity.success" />
        <activity android:name=".Activity.PasswordActivity" />
        <activity android:name=".Activity.ContactinfoActivity" />
        <activity android:name=".Activity.CreateAccount" />
    </application>

</manifest>